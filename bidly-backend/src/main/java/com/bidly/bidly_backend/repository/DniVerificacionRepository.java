package com.bidly.bidly_backend.repository;

import com.bidly.bidly_backend.model.DniVerificacion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DniVerificacionRepository extends JpaRepository<DniVerificacion, Long> {
}
